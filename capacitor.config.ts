import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'app.coreoflife.cof',
  appName: 'Core of Life',
  webDir: '.output/public',
  android: {
    backgroundColor: '#121212',
  },
}

export default config
